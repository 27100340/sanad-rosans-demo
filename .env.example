# Optional. When set, every AI seat answers live through Google Gemini
# (pinned fast models; see src/lib/ai/gemini.ts). When absent, the demo
# uses scripted, deterministic responses and shows "AI scripted" in the top bar.
GEMINI_API_KEY=
